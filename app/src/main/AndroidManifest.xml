<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proteintracker6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InClassRecyclerViewActivity"></activity>
        <activity android:name=".RecyclerViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditActivity" />
        <activity android:name=".MainKelolaActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".Main4FragmentActivity" />
        <activity android:name=".Main3FragmentActivity" />
        <activity android:name=".ProteinTrackerAppActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".Help2Activity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>